# Onboarding

> Generated once, read by every future agent session and every new teammate.
> Keep under ~150 lines. Verify every claim by reading, don't guess from filenames.

## What this is
[Two sentences from the README + entry points.]

## Architecture in 60 seconds
[The flow. Mermaid diagram if more than three moving parts.]

## The 5 commands
See CLAUDE.md — install / run / test / lint / build.

## Where things live
[Top-level dirs → purpose. Only the load-bearing ones.]

## The hot files (by churn)
> `git log --since="6 months ago" --name-only --pretty=format: | sort | uniq -c | sort -rn | head -10`
[Top 10 files, one line each on why they matter — from actually opening them.]

## Conventions & gotchas
See CLAUDE.md. Plus anything discovered the hard way.

## First task suggestion
[A small, real improvement spotted while reading — a good first PR.]
